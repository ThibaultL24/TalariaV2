services:
  db:
    image: postgis/postgis:16-3.4
    ports:
      - "5432:5432"
    environment:
      POSTGRES_PASSWORD: postgres
      POSTGRES_USER: postgres
      POSTGRES_DB: talaria_development
    volumes:
      - pg_data:/var/lib/postgresql/data
volumes:
  pg_data:
